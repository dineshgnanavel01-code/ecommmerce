export default function SalesPieChart({ data }) {
  let cursor = 0;
  const segments = data.map((item, index) => { const start = cursor; cursor += item.value; return `${index === 0 ? "#7c3aed" : index === 1 ? "#10b981" : index === 2 ? "#3b82f6" : "#f59e0b"} ${start}% ${cursor}%`; });
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-bold text-slate-900">Sales distribution</h2><p className="mt-1 text-xs text-slate-400">Revenue by category</p></div><select className="rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs text-slate-500 outline-none"><option>This month</option><option>Last month</option></select></div><div className="mt-7 flex flex-col items-center gap-7 sm:flex-row sm:justify-center"><div className="relative h-44 w-44 shrink-0 rounded-full" style={{ background: `conic-gradient(${segments.join(", ")})` }}><div className="absolute inset-7 grid place-items-center rounded-full bg-white text-center shadow-inner"><div><p className="text-2xl font-black text-slate-900">$284K</p><p className="text-[10px] text-slate-400">Total sales</p></div></div></div><div className="w-full space-y-3 sm:max-w-[170px]">{data.map((item, i) => <div key={item.label} className="flex items-center justify-between gap-3 text-xs"><span className="flex items-center gap-2 text-slate-600"><span className="h-2.5 w-2.5 rounded-full" style={{ backgroundColor: ["#7c3aed", "#10b981", "#3b82f6", "#f59e0b"][i] }}/>{item.label}</span><b className="text-slate-900">{item.value}%</b></div>)}</div></div></section>;
}
